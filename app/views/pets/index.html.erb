<div class="row">

  <div class="col-md offset-md-2">

    <p id="notice"><%= notice %></p>

    <h1>Mascotas</h1><hr>

    <table>
      <thead>
        <tr>
          <th>Nombre</th>
          <th>Raza</th>
          <th>Fecha Nacimiento</th>
          <th>Visitas</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @pets.each do |pet| %>
          <tr>
            <td><%= pet.name %></td>
            <td><%= pet.breed %></td>
            <td><%= pet.birthdate %></td>
            <td> <%= pet.visites %> </td>
            <td><%= link_to 'Show', pet %></td>
            <td><%= link_to 'Edit', edit_pet_path(pet) %></td>
            <td><%= link_to 'Destroy', pet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>




    <%= link_to 'Nueva Atención', new_pet_history_path, class: "btn btn-success" %>
    <%= link_to 'Volver', clients_path, class:"btn btn-light" %>
  </div>
</div>