<div class="row">

  <div class="col-md offset-md-2">
    <p id="notice"><%= notice %></p><hr>
    <p>
      <strong>Cliente:</strong>
      <%= @client.name %>
    </p>
    <p>
      <strong>Telefono:</strong>
      <%= @client.phone %>
    </p>
    <p>
      <strong>Email:</strong>
      <%= @client.email %>
    </p>

      <tbody>
      
        <% @pets.each do |pet| %>
          <tr>
          <td>Nombre:  <%= pet %></td>
          <td>peso máximo:  <%= @weigth_max %></td>
          <td>altura máxima:   <%= @height_max%></td>  
          <%= link_to 'Atenciones', pet, class:"btn btn-link"%> 
          <hr> 
                  
          </tr>
            
        <% end %>
          
      </tbody>
    </table>
    <%= link_to 'Nueva Mascota', new_pet_path(@pet), class: "btn btn-info" %>
    <%= link_to 'Editar', edit_client_path(@client), class: "btn btn-info"%>
    <%= link_to 'Volver', clients_path, class:"btn btn-light" %>
  </div>
</div>
