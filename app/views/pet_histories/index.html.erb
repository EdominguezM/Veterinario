<div class="row">

  <div class="col-md offset-md-1">

    <p id="notice"><%= notice %></p><hr>

    <h1>Historial Mascota</h1>

    <table>
      <thead>
        <tr>
          <th>Peso</th>
          <th>Altura</th>
          <th>Diagnostico</th>
          <th>Mascota</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @pet_histories.each do |pet_history| %>
          <tr>
            <td><%= pet_history.weigth %></td>
            <td><%= pet_history.height %></td>
            <td><%= pet_history.diagnosis %></td>
            <td><%= @pet.name %></td>             
            
            <td><%= link_to 'Mostrar', pet_history %></td>
            <td><%= link_to 'Editar', edit_pet_history_path(pet_history) %></td>
            <td><%= link_to 'Destruir', pet_history, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
          
      </tbody>

      
    </table>

    <br>

    <%= link_to 'Nueva Atención', new_pet_history_path %>
    <%= link_to 'Volver', pets_path %>
  </div>
</div>